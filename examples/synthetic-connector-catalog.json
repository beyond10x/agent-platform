[
  {
    "operation_ref": "support.ticket.list",
    "title": "List support tickets",
    "description": "List support tickets visible through the selected connection.",
    "input_schema": {
      "type": "object",
      "properties": {
        "status": { "type": "string" }
      },
      "additionalProperties": false
    },
    "output_schema": {
      "type": "object",
      "properties": {
        "tickets": { "type": "array" }
      },
      "required": ["tickets"],
      "additionalProperties": false
    },
    "effect": "read_only",
    "approval": "not_required",
    "connections": [
      {
        "connection_ref": "synthetic-support",
        "label": "Synthetic support",
        "provider": "synthetic",
        "audiences": ["support"],
        "purpose": "Local conformance and demonstrations only"
      }
    ],
    "description_ref": "synthetic-description-v1"
  }
]
